Gundu Ata / Sikwin APK backups (timestamped copies).

- APK files here are gitignored (*.apk) so they are not pushed to GitHub — keep a copy on iCloud/Drive/USB if you need off-machine backup.
- Each save includes a matching .sha256 file; verify with: shasum -a -c GunduAta-*.sha256
- To refresh from a new build: copy from website/public/GunduAta.apk or sikwin/app/build/outputs/apk/.../Sikwin-debug.apk
